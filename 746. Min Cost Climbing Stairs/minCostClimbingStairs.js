/**
 * @param {number[]} cost
 * @return {number}
 */
var minCostClimbingStairs = function(cost) {
  let result = 0
  if (cost.length < 2) {
    return result
  } else {
    let last = cost.length - 1
    while (last >= 0) {}
  }

  return result
}
